# Deploy Key — Servidor

**Repo:** https://github.com/flow-social-network/servidor-flow

Cole em **Settings → Deploy keys → Add new**

- **Title:** `flow_servidor`
- **Key:**
```
```

Chave privada: `~/.ssh/flow_servidor` (não compartilhar)