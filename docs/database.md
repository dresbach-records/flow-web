# Banco de dados

PostgreSQL/Prisma mantém identidade, credenciais, sessões e dados que exigem consistência relacional. O schema é aplicado com as rotinas Prisma do backend. Nunca exponha `DATABASE_URL` ao frontend.
