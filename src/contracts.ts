export interface FeedRepository{getForYou():Promise<Post[]>;getFollowing():Promise<Post[]>} export interface PostRepository{like(id:number):Promise<void>;save(id:number):Promise<void>;comment(id:number,text:string):Promise<void>} export interface AuthRepository{login(email:string,password:string):Promise<boolean>;register(email:string,password:string,username:string):Promise<boolean>;logout():Promise<void>} export type Post={id:number,user:string,avatar:string,media:string,caption:string,likes:number,comments:number,liked:boolean,saved:boolean};